
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>

<div id="incidentCard">
    <p id="Title">{{incident.Title}}</p>
    <p id="category">category: {{incident.category}}</p>
    <img src="{{categoryImage}}" alt="Incident image">
    <p id="postedDate">postedDate: {{incident.postedDate}}</p>
    <p id="description">description: {{incident.description}}</p>
    <p id="location">location: {{incident.location}}</p>
    <p id="verified">verified: {{incident.verified}}</p>
    <p id="status">status: {{incident.status}}</p>
    <p id="likes">likes: {{incident.likes}}</p>
    <p id="reportedBy">reportedBy: {{incident.reportedBy}}</p>
    <p>comments:</p>
    <ul id="comments-list">
        {{#each incident.comments}}
            <li>{{this.content}}</li>
        {{/each}}
    </ul>

    <form id="comment-form" name="comment-form" action="/comment/{{incident._id}}" method="POST" class="form">
        <label>
            content
            <input type="text" name="content" id="content" placeholder="Type the content">
        </label>
        <label>
            submit button 
            <button type="submit" name="submit_button" id="submit_button">Submit!</button>
        </label>
    </form>

    <form id="like-form" name="like-form" action="/like/{{incident._id}}" method="POST" class="form">
        <label>
            like button 
            <button type="submit" name="like_button" id="like_button">Add a like to the incident</button>
        </label>
    </form>

    {{#if user}}
    <a id="update_incidentLink" href="/incident_update/{{incident._id}}">Update incident</a>
    <form id="remove-form" name="remove-form" action="/incident_card/{{incident._id}}" method="POST" class="form">
        <label>
            remove button 
            <button type="submit" name="remove_button" id="remove_button">Remove Incident</button>
        </label>
    </form>
    {{/if}}

    {{#if admin}}
        <a id="verifyLink" href="/verify/{{incident._id}}">Verify</a>
        <a id="update_incidentLink" href="/status_update/{{incident._id}}">Update Status</a>
        <form id="remove-form" name="remove-form" action="/incident_card{{incident._id}}" method="POST" class="form">
        <label>
            remove button 
            <button type="submit" name="remove_button" id="remove_button">Remove Incident</button>
        </label>
    </form>
    {{/if}}
    
    <a id="incident_createlink" href="/incident_create">Click Here if you want to create an incident</a>
    <a id="homelink" href="/home">Click Here if you want to return to the home page</a>
</div>
